import 'package:flutter/material.dart';

class CnjView extends StatelessWidget {
  const CnjView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
