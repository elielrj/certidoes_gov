import 'package:flutter/material.dart';

class CadinView extends StatelessWidget {
  const CadinView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
