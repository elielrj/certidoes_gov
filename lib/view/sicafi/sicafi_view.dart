import 'package:flutter/material.dart';


class SicafiView extends StatelessWidget {
  const SicafiView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
